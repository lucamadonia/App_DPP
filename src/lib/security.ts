/**
 * Security constants and helpers
 *
 * Central place for security-related configuration to ensure consistency
 * across all auth flows (sign-up, reset, customer portal, supplier portal).
 */

export const MIN_PASSWORD_LENGTH = 8;
