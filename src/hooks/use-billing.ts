/**
 * Billing hooks — re-exports from BillingContext.
 *
 * Usage:
 *   const { entitlements, checkQuota, hasModule } = useBilling();
 */

export { useBilling, useBillingOptional } from '@/contexts/BillingContext';
